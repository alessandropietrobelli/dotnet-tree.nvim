<Project Sdk="Microsoft.NET.Sdk">
  <!-- Says so itself, with no test SDK reference of its own: the property is
       the more direct signal and does not need the package. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>
</Project>
