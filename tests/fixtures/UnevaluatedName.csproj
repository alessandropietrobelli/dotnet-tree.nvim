<Project Sdk="Microsoft.NET.Sdk">
  <!-- Runnable, but the output file name is an MSBuild property reference.
       Reading XML cannot resolve it, so the dll path cannot be composed. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <AssemblyName>$(ToolName)</AssemblyName>
  </PropertyGroup>
</Project>
