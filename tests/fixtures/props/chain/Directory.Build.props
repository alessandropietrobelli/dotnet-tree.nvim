<Project>

  <!-- Top of the chain. net9.0, not the net10.0 of the props file two
       directories up, so a test can tell which file the framework came from. -->
  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
  </PropertyGroup>

</Project>
